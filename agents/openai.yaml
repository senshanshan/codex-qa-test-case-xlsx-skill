interface:
  display_name: "Test Case XLSX Generator"
  short_description: "Generate QA test cases or revise them from requirement changes using payload JSON."
  default_prompt: "Use $test-case-xlsx-generator to generate categorized QA test cases from this PRD, screenshot, or feature description, or revise the latest workbook and payload JSON in my workspace when the requirement changes."
