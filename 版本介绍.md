# 版本介绍

这里记录项目各版本的主要变化，方便快速了解版本差异。

## v3.0.0

- 引入固定 `payload.json` 模板 `templates/base_payload.json`
- 将“首次生成”和“需求变更后的增量修改”明确拆分为两条固定流程
- 新增 `scripts/update_cases_from_change.py`，用于基于已有 payload 生成下一版修订文件
- 后续修改默认依赖工作区中的 workbook 与 payload JSON 成对存在
- 支持在 payload 的 case 中保留 `_meta.source_requirement` 与 `_meta.keywords` 作为内部命中锚点
- workbook 继续只保留用户可见列，不展示 `_meta`
- 本次改动主要目的是：降低token消耗（避免遇到需求更改时，需要重新生成所有case），提高效率（有模板json可以避免每次都重新生成payload.json）

## v2.0.0

- 强化边界值生成规则，明确覆盖最小值、最小值减一、最大值、最大值加一、正常值和空值等典型边界场景
- 通过 `extra_columns` 追加的可选列，满足不同使用者，不同人员的使用要求
- 新增中文示例文件，覆盖通用提示词示例和执行版工作簿模板示例
- 同步更新 `README.md`、`SKILL.md`、`references/classification-rules.md`、`references/workbook-schema.md` 和 `references/input-contract.md`
- 未修改默认工作簿结构和导出脚本核心行为

## v1.0.0

- 初始开源版本
