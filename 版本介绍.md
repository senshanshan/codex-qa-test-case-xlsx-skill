# 版本介绍

这里记录项目各版本的主要变化，方便使用者快速了解版本之间的差异。

## v2.0.0
根据使用反馈，修改了以下内容：
- 强化了边界值生成规则，明确覆盖最小值、最小值-1、最大值、最大值+1、正常值和空值等典型边界场景。
- 明确 `实际结果` 等执行类字段不是默认列，而是通过 `extra_columns` 追加的可选列。
因为该skill目的是做一个前置的用例生成辅助
- 由使用者提出反馈，新增中文示例文件，分别覆盖通用提示词示例和执行版工作簿模板示例。
- 同步更新 `README.md`、`SKILL.md`、`references/classification-rules.md`、`references/workbook-schema.md` 和 `references/input-contract.md`，保证规则说明一致。
- 本版本没有修改默认工作簿结构，也没有修改导出脚本行为。

## v1.0.0

- 初始开源版本。
